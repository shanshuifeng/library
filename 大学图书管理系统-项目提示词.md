# 大学图书管理系统 - 项目提示词

## 📋 项目概述

构建一个完整的大学图书管理系统，包含前后端分离架构：
- **后端**: Python + Flask + postgresql + SQLAlchemy
- **前端**: Vue 3 + Element Plus + Vite
- **包管理**: uv (Python), npm/pnpm (Node.js)

---

## 🎯 核心功能模块

### 1. 用户管理
- 多角色支持：管理员、教师、学生
- 登录/注册/退出
- 个人信息管理
- 密码修改与重置

### 2. 图书管理
- 图书信息 CRUD（书名、作者、ISBN、出版社、出版日期、分类、库存、价格、封面）
- 图书分类管理（多级分类树）
- 图书检索（按书名、作者、ISBN、分类）
- 库存管理与预警

### 3. 借阅管理
- 借书/还书流程
- 借阅记录查询
- 续借功能（限制续借次数）
- 逾期罚款计算
- 借阅权限设置（不同角色借阅数量限制）

### 4. 读者管理
- 读者信息维护
- 借阅证管理
- 读者分类（学生、教师、管理员）

### 5. 系统管理
- 系统参数配置
- 数据统计与报表
- 操作日志
- 数据备份

---

## 🗄️ 数据库设计

### 核心表结构

```sql
-- 用户表
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    email VARCHAR(100),
    phone VARCHAR(20),
    role ENUM('admin', 'teacher', 'student') DEFAULT 'student',
    status TINYINT DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 图书表
CREATE TABLE books (
    id INT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(200) NOT NULL,
    author VARCHAR(100),
    isbn VARCHAR(20) UNIQUE,
    publisher VARCHAR(100),
    publish_date DATE,
    category_id INT,
    price DECIMAL(10, 2),
    stock INT DEFAULT 0,
    description TEXT,
    cover_image VARCHAR(500),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 图书分类表
CREATE TABLE categories (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    parent_id INT DEFAULT 0,
    level INT DEFAULT 1,
    sort_order INT DEFAULT 0
);

-- 借阅记录表
CREATE TABLE borrow_records (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    book_id INT NOT NULL,
    borrow_date DATE NOT NULL,
    due_date DATE NOT NULL,
    return_date DATE,
    renew_count INT DEFAULT 0,
    fine DECIMAL(10, 2) DEFAULT 0,
    status ENUM('borrowed', 'returned', 'overdue') DEFAULT 'borrowed',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (book_id) REFERENCES books(id)
);

-- 系统配置表
CREATE TABLE system_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    config_key VARCHAR(50) UNIQUE NOT NULL,
    config_value VARCHAR(500),
    description VARCHAR(200)
);
```

---

## 🔌 API 设计

### 基础路径: `/api/v1`

### 认证相关
```
POST   /auth/login          # 登录
POST   /auth/register       # 注册
POST   /auth/logout         # 退出
GET    /auth/profile         # 获取当前用户信息
PUT    /auth/password        # 修改密码
```

### 图书管理
```
GET    /books               # 获取图书列表（分页、搜索）
GET    /books/:id           # 获取图书详情
POST   /books               # 新增图书
PUT    /books/:id           # 更新图书
DELETE /books/:id           # 删除图书
GET    /books/categories    # 获取图书分类
```

### 借阅管理
```
POST   /borrows             # 借书
PUT    /borrows/:id/return  # 还书
PUT    /borrows/:id/renew   # 续借
GET    /borrows             # 借阅记录列表
GET    /borrows/user/:id    # 用户借阅记录
```

### 用户管理
```
GET    /users               # 用户列表
POST   /users               # 创建用户
PUT    /users/:id           # 更新用户
DELETE /users/:id           # 删除用户
```

### 统计报表
```
GET    /stats/overview      # 系统概览
GET    /stats/borrow-trend  # 借阅趋势
GET    /stats/popular-books # 热门图书
```

---

## 📁 项目结构

```
book-manager/
├── backend/                    # 后端项目
│   ├── app/
│   │   ├── __init__.py
│   │   ├── config.py          # 配置文件
│   │   ├── models/            # 数据模型
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── book.py
│   │   │   ├── borrow.py
│   │   │   └── category.py
│   │   ├── routes/            # 路由
│   │   │   ├── __init__.py
│   │   │   ├── auth.py
│   │   │   ├── book.py
│   │   │   ├── borrow.py
│   │   │   └── user.py
│   │   ├── services/          # 业务逻辑
│   │   │   ├── __init__.py
│   │   │   ├── auth_service.py
│   │   │   ├── book_service.py
│   │   │   └── borrow_service.py
│   │   ├── utils/             # 工具函数
│   │   │   ├── __init__.py
│   │   │   ├── auth.py
│   │   │   └── response.py
│   │   └── extensions.py      # Flask 扩展
│   ├── migrations/            # 数据库迁移
│   ├── tests/                 # 测试
│   ├── .env                   # 环境变量
│   └── run.py                 # 启动入口
│
├── frontend/                  # 前端项目
│   ├── src/
│   │   ├── api/               # API 请求
│   │   ├── assets/            # 静态资源
│   │   ├── components/        # 公共组件
│   │   ├── router/            # 路由配置
│   │   ├── store/             # 状态管理 (Pinia)
│   │   ├── views/             # 页面组件
│   │   │   ├── auth/
│   │   │   ├── book/
│   │   │   ├── borrow/
│   │   │   └── admin/
│   │   ├── utils/             # 工具函数
│   │   ├── App.vue
│   │   └── main.js
│   ├── public/
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
│
└── README.md
```

---

## 🛠️ 技术实现要点

### 后端 (Flask)

#### 1. 项目初始化
```bash
# 使用 uv 创建虚拟环境
uv venv
uv pip install flask flask-sqlalchemy flask-migrate flask-cors flask-jwt-extended pymysql
```

#### 2. 核心配置 (config.py)
```python
import os
from datetime import timedelta

class Config:
    SECRET_KEY = os.getenv('SECRET_KEY', 'dev-secret-key')
    SQLALCHEMY_DATABASE_URI = os.getenv('DATABASE_URL', 'mysql+pymysql://root:password@localhost:3306/book_manager')
    SQLALCHEMY_TRACK_MODIFICATIONS = False
    JWT_SECRET_KEY = os.getenv('JWT_SECRET_KEY', 'jwt-secret-key')
    JWT_ACCESS_TOKEN_EXPIRES = timedelta(hours=1)
```

#### 3. 响应格式
```python
def success_response(data=None, message='Success'):
    return {
        'code': 200,
        'message': message,
        'data': data
    }

def error_response(message='Error', code=400):
    return {
        'code': code,
        'message': message,
        'data': None
    }
```

### 前端 (Vue 3)

#### 1. 项目初始化
```bash
npm create vite@latest frontend -- --template vue
cd frontend
npm install element-plus axios pinia vue-router
```

#### 2. API 请求封装 (api/request.js)
```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'

const request = axios.create({
  baseURL: '/api/v1',
  timeout: 10000
})

request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  }
)

request.interceptors.response.use(
  response => response.data,
  error => {
    ElMessage.error(error.response?.data?.message || '请求失败')
    return Promise.reject(error)
  }
)

export default request
```

#### 3. 路由配置 (router/index.js)
```javascript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/auth/Login.vue')
  },
  {
    path: '/',
    component: () => import('@/views/layout/Layout.vue'),
    children: [
      {
        path: '',
        name: 'Home',
        component: () => import('@/views/Home.vue')
      },
      {
        path: 'books',
        name: 'Books',
        component: () => import('@/views/book/BookList.vue')
      },
      // ... 其他路由
    ]
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

---

## 📊 功能清单

### 管理员功能
- [ ] 用户管理（增删改查、角色分配）
- [ ] 图书管理（增删改查、批量导入）
- [ ] 分类管理（树形结构）
- [ ] 借阅管理（借书、还书、续借、罚款）
- [ ] 统计报表（借阅趋势、热门图书、库存预警）
- [ ] 系统设置（参数配置）

### 教师/学生功能
- [ ] 图书检索（多条件搜索）
- [ ] 图书详情查看
- [ ] 借阅操作
- [ ] 借阅记录查询
- [ ] 个人中心（借阅历史、续借）
- [ ] 密码修改

---

## 🚀 开发步骤

### 第一阶段：环境搭建
1. 初始化项目结构
2. 配置数据库连接
3. 搭建前端项目框架
4. 配置跨域代理

### 第二阶段：用户模块
1. 实现用户注册/登录 API
2. 实现 JWT 认证
3. 前端登录页面
4. 路由守卫

### 第三阶段：图书模块
1. 图书 CRUD API
2. 图书分类 API
3. 前端图书列表页面
4. 图书搜索功能

### 第四阶段：借阅模块
1. 借阅/还书/续借 API
2. 借阅记录查询
3. 前端借阅管理页面
4. 逾期罚款计算

### 第五阶段：统计与优化
1. 统计报表 API
2. 数据可视化
3. 性能优化
4. 部署上线

---

## 📝 开发规范

### Git 提交规范
```
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式
refactor: 重构
test: 测试
chore: 构建/工具
```

### 代码规范
- 后端遵循 PEP 8
- 前端遵循 Vue 3 风格指南
- 使用 ESLint + Prettier
- 中文注释

---

## 🔧 环境要求

### 后端
- Python 3.10+
- uv (包管理器)
- postgresql 42.7+

### 前端
- Node.js 18+
- npm / pnpm

---

## 📚 参考资料

- [Flask 官方文档](https://flask.palletsprojects.com/)
- [SQLAlchemy 文档](https://docs.sqlalchemy.org/)
- [Vue 3 官方文档](https://vuejs.org/)
- [Element Plus 文档](https://element-plus.org/)
